<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.QuranHabit" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/accent</item>
        <item name="colorPrimaryVariant">@color/accent</item>
        <item name="colorOnPrimary">@color/text_primary</item>

        <!-- Background colors -->
        <item name="android:windowBackground">@color/primary_dark</item>
        <item name="android:colorBackground">@color/primary_dark</item>

        <!-- Button styles -->
        <item name="materialButtonStyle">@style/Widget.QuranHabit.Button</item>

        <!-- action bar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Widget.QuranHabit.Button" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/accent</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="cornerRadius">16dp</item>
    </style>
</resources>