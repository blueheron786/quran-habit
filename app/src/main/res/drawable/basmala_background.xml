<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent"/>
    <!-- background -->
    <solid android:color="@color/reddish_brown"/>

    <stroke
        android:width="1dp"
        android:color="@color/gold" />
    <!-- Tight, symmetrical padding built into shape -->
    <padding
        android:top="2dp"
        android:bottom="2dp"
        android:left="8dp"
        android:right="8dp"/>
</shape>